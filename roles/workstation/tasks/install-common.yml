---
- name: Install gems
  gem:
    name: "{{ item }}"
    state: "{{ packages_state }}"
  with_items:
    - ghi

- name: Install eslint
  npm:
    name: eslint
    global: yes
    state: "{{ packages_state }}"
